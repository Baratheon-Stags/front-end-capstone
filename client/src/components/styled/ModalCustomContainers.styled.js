import styled from 'styled-components';
import FlexContainer from './FlexContainer.styled';

const ItemCompareContainer = styled(FlexContainer)`
  margin-bottom: 20px;
`;

export default ItemCompareContainer;
